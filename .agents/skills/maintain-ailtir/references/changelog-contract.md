# Changelog contract

The root `CHANGELOG.md` is authoritative for Ailtir downstream state. It has:

- a generated current-state block between `AILTIR-CURRENT` markers;
- an active patch table with public issue or PR provenance;
- newest-first dated history entries.

Git, ECR, infrastructure, ECS, and the live health endpoint remain the
authoritative evidence behind the recorded values.
